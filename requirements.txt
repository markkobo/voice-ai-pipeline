# Core
fastapi>=0.100.0
uvicorn>=0.23.0
pydantic>=2.0.0
websockets>=12.0
prometheus-client>=0.19.0
openai>=1.0.0
pydub>=0.25.0

# PyTorch (CUDA 12.4) — 2.6.0+ required for torch.multinomial + CUDA graphs compatibility
# Install with: pip install torch>=2.5.0 torchaudio torchvision --index-url https://download.pytorch.org/whl/cu124
torch>=2.5.0
torchaudio>=2.5.0
torchvision>=0.20.0

# ASR
qwen-asr>=0.0.6

# TTS
faster-qwen3-tts
qwen-tts>=0.1.1

# Testing
pytest>=7.4.0
pytest-asyncio>=0.21.0
httpx>=0.24.0
