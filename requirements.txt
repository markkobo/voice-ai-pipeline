# Core
fastapi>=0.100.0
uvicorn>=0.23.0
pydantic>=2.0.0
websockets>=12.0
prometheus-client>=0.19.0
openai>=1.0.0
pydub>=0.25.0
python-dotenv>=1.0.0

# PyTorch CUDA 12.4 — MUST use exact version for CUDA graphs compatibility
# Install FIRST: pip install torch==2.6.0+cu124 torchaudio==2.6.0+cu124 --index-url https://download.pytorch.org/whl/cu124
torch==2.6.0+cu124
torchaudio==2.6.0+cu124
torchvision>=0.20.0

# ASR - Real-time streaming for WebSocket voice chat
qwen-asr>=0.0.6

# TTS - Voice synthesis (FasterQwen3TTS with CUDA graphs)
faster-qwen3-tts
qwen-tts>=0.1.1

# Audio Processing Pipeline
# NOTE: pyannote.audio upgrades torch to latest, breaking CUDA graphs
# Install order: pip install faster-whisper speechbrain pyannote.audio noisereduce
# Then restore torch: pip install torch==2.6.0+cu124 torchaudio==2.6.0+cu124 --index-url https://download.pytorch.org/whl/cu124
faster-whisper>=1.0.0
speechbrain>=1.0.0
pyannote.audio>=3.0.0
noisereduce>=3.0.0

# LoRA Fine-tuning (M4)
transformers>=4.40.0
peft>=0.10.0
datasets>=2.18.0
accelerate>=0.28.0

# Testing
pytest>=7.4.0
pytest-asyncio>=0.21.0
httpx>=0.24.0

# System dependencies (install via apt/apt-get):
# ffmpeg - audio format conversion (sudo apt install ffmpeg)
# rnnoise - noise reduction (sudo apt install rnnoise)
