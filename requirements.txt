# Core
fastapi>=0.100.0
uvicorn>=0.23.0
pydantic>=2.0.0
websockets>=12.0
prometheus-client>=0.19.0
openai>=1.0.0
pydub>=0.25.0
python-dotenv>=1.0.0

# PyTorch CUDA 12.4 — MUST use exact version for CUDA graphs compatibility
# Install with: pip install torch==2.6.0 torchaudio==2.6.0 --index-url https://download.pytorch.org/whl/cu124
# The +cu124 suffix only works with PyTorch's index URL
torch==2.6.0
torchaudio==2.6.0
torchvision>=0.20.0

# ASR - Real-time streaming for WebSocket voice chat
qwen-asr>=0.0.6

# TTS - Voice synthesis (FasterQwen3TTS with CUDA graphs)
faster-qwen3-tts
qwen-tts>=0.1.1

# Audio Processing Pipeline
# NOTE: pyannote.audio 4.x requires torch>=2.8.0 but works with torch 2.6.0+cu124
# when using waveform dict approach (bypasses torchcodec). After installing pyannote.audio,
# restore torch: pip install torch==2.6.0+cu124 torchaudio==2.6.0+cu124 --index-url https://download.pytorch.org/whl/cu124
# NOTE: faster-whisper uses large-v3 model for Chinese transcription (RTF ~0.006, excellent fluency)
faster-whisper>=1.0.0
speechbrain>=1.0.0
pyannote.audio==4.0.4
noisereduce>=3.0.0

# LoRA Fine-tuning (M4)
transformers>=4.40.0
peft>=0.10.0
datasets>=2.18.0
accelerate>=0.28.0
soundfile>=0.12.0

# Testing
pytest>=7.4.0
pytest-asyncio>=0.21.0
httpx>=0.24.0

# System dependencies (install via apt/apt-get):
# ffmpeg - audio format conversion (sudo apt install ffmpeg)
# rnnoise - noise reduction (sudo apt install rnnoise)
